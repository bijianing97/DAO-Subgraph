{
  "name": "chainmonitor",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ chainMonitorPrison",
    "create-local": "graph create --node http://localhost:8777/ chainMonitorPrison",
    "remove-local": "graph remove --node http://localhost:8777/ chainMonitorPrison",
    "deploy-local": "graph deploy --node http://localhost:8777/ --ipfs http://localhost:5001 chainMonitorPrison",
    "one": "graph remove --node http://localhost:8777/ chainMonitorPrison && graph codegen --output-dir src/types/ && graph build && graph create --node http://localhost:8777/ chainMonitorPrison && graph deploy --node http://localhost:8777/ --ipfs http://localhost:5001 chainMonitorPrison",
    "clean": "rm -rf build/ && rm -rf src/types/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.37.1",
    "@graphprotocol/graph-ts": "^0.29.0",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.5.1",
    "typescript": "^4.9.4"
  }
}
